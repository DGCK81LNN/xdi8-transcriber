{
  "name": "xdi8-transcriber-root",
  "private": true,
  "workspaces": [
    "package",
    "site"
  ],
  "scripts": {
    "build": "npm run --workspace=package build && npm run --workspace=site build",
    "build:package": "npm run --workspace=package build",
    "build:site": "npm run --workspace=package build:data && npm run --workspace=site build"
  }
}
